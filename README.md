Build [vincentqb.github.io](https://vincentqb.github.io) with `uv run --script build.py`.

`data.yaml` is a generated copy — the canonical source lives in a private repo and is synced in with `just site` there; edit it there, not here.
